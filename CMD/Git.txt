git add .
git commit -m ""
git push
git pull --rebase
git checkout XXX --XX.py
--------
close  CRLF
git config --global core.autocrlf false

Restore
git checkout 95f438ffc26095d0df78a7bed2e799a391ba2fa7 -- PMS.db

# 1. 設定全域使用者名稱
git config --global user.name "bfc8g4v63"

# 2. 設定全域使用者 Email
git config --global user.email "x0925234139@gmail.com"

# 3. 進入你的專案資料夾
cd "C:\Users\user\Desktop\Nelson\Dev\Develop program"

# 4. 初始化 Git 倉庫（讓這個資料夾變成 Git 專案）
git init

# 5. 把所有檔案加入暫存區（準備提交）
git add .

# 6. 準備提交（commit）
git commit -m "Initial commit for PMS"
git commit -m "之後更新內容管控備註"

# 7. 新增遠端倉庫（origin 指向 GitHub 上的專案）
git remote add origin 

# 8. 把本地分支改名成 main（GitHub預設是 main，不是 master）
git branch -M main

# 9. 把本地 main 分支推送到遠端 origin（上傳到 GitHub）
git push -u origin main

# 10. 查看現在設定的遠端倉庫資訊
git remote -v

# 11. 進入該文件夾移除.git (git init)
rmdir /s /q .git

